package pizzeria.model.enums;

/**
 * Define el grosor de la masa disponible en la pizzería.
 * Encapsula esta propiedad para evitar el uso de Strings libres
 * que podrían generar errores en tiempo de ejecución.
 */
public enum GrosorMasa {
    DELGADA,
    NORMAL,
    GRUESA
}
